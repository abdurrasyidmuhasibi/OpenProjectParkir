<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.parkir">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SettingLogout"
            android:screenOrientation="portrait"></activity>
        <activity android:name=".activities.Success"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.DetailPembayaran"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.QrCodeScanner"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.QrCode"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.HomeKangParkir"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.HomeUser"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.LoginPage"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>